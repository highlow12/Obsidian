```dataviewjs
dv.pages('"2. 에어리어/커피리뷰"') .where(p => p.file.name != dv.current().file.name) // 현재 문서는 제외합니다. 
.forEach(p => { 
const content = `#### 최근 이 커피와 같은 점수를 준 커피들 
\`\`\`dataview 
table date, rate 
FROM "" 
WHERE file.folder = this.file.folder 
AND rate = this.rate 
AND file.name != this.file.name 
SORT file.mtime DESC 
LIMIT 5 
\`\`\` `; 
dv.paragraph(content, p.file.path); // 각 파일에 내용을 추가합니다. 
			  });
```
